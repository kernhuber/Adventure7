# Claude memory snapshot

A point-in-time copy (2026-06-24) of Claude Code's per-project memory notes, which
live outside the repo under
`~/.claude/projects/<project>/memory/`. Snapshotted here so the continuity notes
are backed up on GitHub.

These are a **snapshot**, not the live files — the authoritative memory is the one
in `~/.claude`. Re-copy if you want to refresh this snapshot. See `MEMORY.md` for
the index.
